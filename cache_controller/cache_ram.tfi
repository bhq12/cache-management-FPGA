


// Instantiate the module
cache_ram instance_name (
    .clk(clk), 
    .address(address), 
    .data_in(data_in), 
    .data_out(data_out), 
    .enable(enable), 
    .rw(rw), 
    .hit_miss(hit_miss), 
    .output_ready(output_ready)
    );


